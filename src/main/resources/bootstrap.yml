spring:
  application:
    name: config-client
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server-github
#      uri: http://localhost:8881
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka